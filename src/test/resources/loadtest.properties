# Properties for Ruffman's Long Term Stress Test

# Duration of test in seconds
duration=2

# Number of concurrent clients (0 < n < 100)
clients=2

# Uploads per minute (> 0)
uploadsPerMin=30

# Downloads per minute (> uploadsPerMin)
downloadsPerMin=30

# File size in kB (> 0)
fileSizeKB=2

# Overwrite ratio in % (0 <= n <= 100)
overwriteRatio=80